"use strict";

const LoadRemoteRepo = ({ formHandler }) => {
  return (
    <div className="remoteRepo">
      <h1>Load a remote repo</h1>

      {/* Input for username/repoName or url then fire ahead */}

      {/* Path to save the repo */}
    </div>
  );
};

module.exports = LoadRemoteRepo;
